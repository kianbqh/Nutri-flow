# ── Core framework ───────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.42.0
python-multipart==0.0.22  # >=0.0.22 fixes arbitrary file write (< 0.0.22) and DoS (< 0.0.18)
pydantic==2.11.0
pydantic-settings==2.7.1

# ── MCP (Model Context Protocol) ─────────────────────────────────────────────
mcp==1.23.0  # >=1.23.0 fixes DNS rebinding (< 1.23.0); >=1.9.4 fixes FastMCP DoS; >=1.10.0 fixes HTTP DoS

# ── Deep learning ────────────────────────────────────────────────────────────
torch==2.6.0      # >=2.6.0 fixes RCE via torch.load weights_only=True (< 2.6.0)
torchvision==0.21.0  # compatible with torch 2.6.0
timm==1.0.3          # Swin Transformer pretrained weights
einops==0.8.0        # tensor manipulation helpers

# ── Computer Vision utilities ────────────────────────────────────────────────
opencv-python-headless==4.9.0.80
Pillow==12.1.1  # >=12.1.1 fixes out-of-bounds write on PSD images (>= 10.3.0, < 12.1.1)
numpy==1.26.4
pycocotools==2.0.8   # COCO mask utilities

# ── HTTP client (for downloading images from OSS pre-signed URLs) ─────────────
httpx==0.27.2

# ── Observability ────────────────────────────────────────────────────────────
structlog==24.1.0

# ── Training utilities ───────────────────────────────────────────────────────
tensorboard==2.17.0           # TensorBoard visualization for training metrics
matplotlib==3.9.2              # visualization and plotting
scipy==1.14.1                  # scientific computing utilities
albumentations==1.4.11         # data augmentation library for CV
